function OpenDialogWin(url,width,height)
{
	//var isrefesh = window.showModalDialog(url,'','dialogWidth:'+width+'px;status:no;dialogHeight:'+height+'px');
	//if (isrefesh)
	//{
	//	window.location += ' ';
	//}
	window.location.href=url;
}

function SetReturnValue(ReturnValue,IsWinClose)
{
	window.top.returnValue = ReturnValue;
	if (IsWinClose)
	{
		window.close();
	}
} 

function OpenModalDialogmain(url,width,height)
{
	var isrefesh = window.showModalDialog(url,'','dialogWidth:'+width+'px;dialogHeight:'+height+'px');
	if (isrefesh)
	{
		window.location += ' ';
	}
}